# birdnetgo-friend
Birdnetgo scripts

friend-birdnetgo.yml is the ansible setup script. config-files are called by the ansible script.

shelly-smart-plug-auto-restart.js is the script that should be pasted into the web UI script editor of the shelly smart plug powering the device.
